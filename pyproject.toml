[project]
name = "ripadb"
version = "0.1.0"
description = "Download, store, analyze, and visualize California RIPA stop data"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "pyyaml",
    "polars[calamine]",
    "psycopg[binary]",
    "psycopg-pool",
    "fastapi",
    "uvicorn[standard]",
    "jinja2",
    "tabulate>=0.9.0",
    "markdown>=3.5",
    "census",
    "us",
    "python-dotenv",
]

[tool.hatch.build.targets.wheel]
packages = []
